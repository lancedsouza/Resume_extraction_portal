services:
  redis:
    image: redis:alpine
  
  app:
    build: .
    command: streamlit run app.py
    ports: ["8501:8501"]
    volumes: [".:/app", "./data:/app/data"]
  
  worker:
    build: .
    command: python worker.py
    volumes: [".:/app", "./data:/app/data"]
    depends_on: [redis]